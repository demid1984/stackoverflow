spring:
  application:
    name: hibernate6
  datasource:
    url: ${JDBC_URL}
    username: ${JDBC_USER}
    password: ${JDBC_PASSWORD}
  jpa:
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/changelog.xml
